stages:
  - build
  - testing

install:
    stage: build
    script:
        - ls
        - pip install virtualenv
        - cd setup-scripts
        - bash setup-venv.sh
        - pip install tox

testing:
    stage: testing
    script:
        - tox -e .

