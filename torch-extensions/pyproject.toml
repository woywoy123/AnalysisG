[build-system]
requires = ["setuptools", "wheel", "pip", "torch==1.13.1"]
build-backend = "setuptools.build_meta"

[project]
name = "AnalysisTopGNN-Extensions"
version = "1.1"
readme = "README.md"
authors = [{name = "woywoy123"}]
